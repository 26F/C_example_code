
/*

stdin  in
stdout out
stderr error_logging

A buffer is a temporary storage area that uses streams to move and store data.

fopen()
fclose()

*/

int main() {
  // FILE *fp;
  // fp = fopen("names.txt","r");
  // loads file data into a buffer.
  // fopen() creates a stream and the buffer is associated with that stream.
  // fp is a pointer to the base address of the stream.

  // r read mode. Open the file if it exist, else return a NULL pointer.
  // w write mode. Overwrite the file. 
  // a append mode. returns a NULL pointer if the file does not exist.

  // add a + to create a new file, for example w+ .

  // using wb is fine for writing new files.

}
